spring:
  application:
    name: demo
  datasource:
    url: jdbc:mariadb://localhost:3306/demo
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
